import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    mainContainer: {},
    btnContainerStyle: {
        flex:1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    buttonTextStyle: {
        fontSize:24,
        fontFamily: "hello",
        fontWeight: 'bold',
        color:'red'
    }

});